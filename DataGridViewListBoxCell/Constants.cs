using CustomDataGridViewControls.Custom;

namespace CustomDataGridViewControls
{
    internal class Constants
    {
        private readonly DialogItem dialogItem = new(0, "");

        public const string DIALOG_DISP_DATA = nameof(dialogItem.DispData);
    }
}
